<?php declare(strict_types=1);

// NOTE: This file is autogenerated, please do not edit manually.

namespace {{namespace}};

final class {{name}} implements JsonSerializable
{
    {{#each properties}}
    /**
     * @var {{this.type}}
     */
    private ${{this.name}};
    {{/each}}

    public function __construct(
        {{#each properties}}
        ?{{this.type}} ${{this.name}}
        {{/each}}
    ) {
        {{#each properties}}
        $this->{{this.name}} = ${{this.name}};
        {{/each}}
    }

    public function jsonSerialize()
    {
        return [
            {{#each properties}}
            '{{this.name}}' => $this->{{this.name}},
            {{/each}}
        ];
    }

    public static function jsonDeserialize(string $json): self
    {
        $decoded = json_decode($json, true);

        return new self(
            {{#each properties}}
            $decoded['{{this.name}}'] ?? null,
            {{/each}}
        );
    }

    {{#each properties}}
    public function {{this.name}}(): {{this.type}}
    {
        return $this->{{this.name}};
    }
    {{/each}}
}
